import 'package:flutter/material.dart';

class Sinister extends StatelessWidget {
  const Sinister({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}